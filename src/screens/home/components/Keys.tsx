import {View, StyleSheet} from 'react-native';
import React from 'react';
import {Text, Layout} from '@ui-kitten/components';

const Keys: React.FC = (): React.JSX.Element => {
  return (
    <Layout>
      <Text>Keys</Text>
    </Layout>
  );
};

export default Keys;

const styles = StyleSheet.create({});
